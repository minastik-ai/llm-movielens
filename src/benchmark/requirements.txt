# Benchmark — Dependencies
# Python 3.10+

# Deep learning
# Upper bounds are deliberate. Unpinned majors have already broken this
# project once: anthropic 1.x removed `temperature` from messages.create(),
# so the generator could no longer reproduce the sampling the paper states.
# torch / sentence-transformers move the EMBEDDINGS, which are the paper's
# features, so a drift there moves every number for anyone who regenerates
# rather than downloading. Pinned 2026-09-02.
torch>=2.0.0,<3.0
numpy>=1.24.0,<3.0
pandas>=2.1.0,<4.0
scipy>=1.10.0,<2.0

# Sentence transformers (for BERT title baseline)
sentence-transformers>=2.2.0,<6.0

# Evaluation & analysis
scikit-learn>=1.3.0,<2.0
tqdm>=4.65.0,<5.0
